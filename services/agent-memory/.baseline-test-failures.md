# Baseline test failures

There are currently no waived baseline test failures.

The drift-detection test `deploy/baseline_failures_test.go` still
runs the repo-wide package set and asserts that any failing test is
either listed in `expectedBaselineFailures` or fixed. Keep this file
and that map in sync if a future workstream intentionally waives a
pre-existing failure.
