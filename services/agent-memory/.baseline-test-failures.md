# Baseline test failures

The drift-detection test `deploy/baseline_failures_test.go` runs the
repo-wide package set and asserts that any failing test is either
listed in `expectedBaselineFailures` or fixed. Keep this file and
that map in sync if a future workstream intentionally waives a
pre-existing failure.

## Waived failures

None at present. The previously-waived
`TestCppFixture_EmitsExpectedNodeAndEdgeSet` and
`TestCSharpFixture_EmitsExpectedNodeAndEdgeSet` entries were
removed in iter-4 of the Go-fixture-test stage once
`parser_treesitter_cpp.go` and `parser_treesitter_csharp.go` landed
their respective walkers as a cross-cutting baseline repair
authorised by the operator's pinned answer to the
`baseline-package-break` open question.

