# p0-mixed-langs sample repo

Single-file modules covering the four parser-fleet languages
(Go / Python / TypeScript / Java). The scenario assertion in
`../run.sh` verifies all four show up in `RunArtifact.Files`
after `cleanc analyze .` runs against this tree.
