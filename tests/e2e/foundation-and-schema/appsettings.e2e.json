{
  "_comment": "docs/forge/e2e.md Stage 7.3 — appsettings template for phase 'foundation-and-schema' (setup: inline). Populate values via env-vars on CI or the operator's secrets store; empty values trigger SetupSkippedException locally.",
  "E2e": {
    "_comment": "Inline setup — no external env-vars required."
  }
}