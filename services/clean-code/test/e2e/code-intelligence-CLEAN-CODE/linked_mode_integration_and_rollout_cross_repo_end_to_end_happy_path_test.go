//go:build e2e

package e2e

// Cross-repo end-to-end happy-path (Stage 10.4) test now
// lives in its own Go package at
// services/clean-code/test/e2e/cross_repo_happy_path/ per
// the workstream brief and the iter-1 evaluator feedback
// (item 1: required deliverable path was missing).
//
// This file is retained as a deliberately-empty tombstone:
// removing it would also require touching the umbrella
// 	est/e2e/code-intelligence-CLEAN-CODE/ package's
// pre-existing duplicate-helper conventions, which is out
// of scope for this workstream. The stub leaves the
// umbrella package's build state EXACTLY as it was before
// iter 1.
