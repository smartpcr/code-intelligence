# Baseline test failures

The drift-detection test `deploy/baseline_failures_test.go` runs the
repo-wide package set and asserts that any failing test is either
listed in `expectedBaselineFailures` or fixed. Keep this file and
that map in sync if a future workstream intentionally waives a
pre-existing failure.

## Waived failures

### `internal/repoindexer/ast` — sibling parser-stage stubs

| Test | Owner stage | Why it fails today |
| --- | --- | --- |
| `TestCppFixture_EmitsExpectedNodeAndEdgeSet` | C++ parser stage (`phase-cpp-parser-stage-cpptreesitterparser-implementation`) | `parser_treesitter_cpp.go` is an intentional partial — its file-level doc comment declares "Methods, free functions, calls, and includes are out of scope here; sibling stages own them." The fixture test asserts the finished walker emits `Base.identify`, `Greeter.greet`, and `log_global` MethodDecls plus their static_calls; the current walker emits 0 methods. |
| `TestCSharpFixture_EmitsExpectedNodeAndEdgeSet` | C# parser stage (`phase-csharp-parser-stage-csharptreesitterparser-implementation`) | `parser_treesitter_csharp.go` is an explicit stub: its file header reads "PLACEHOLDER landed by the Go-parser stage; SIBLING STAGE WORKSTREAM owns the full C# implementation." The fixture test asserts class/method/inheritance extraction; the stub `Parse()` returns an empty `ParseResult{}`. |

These waivers MUST be removed when the corresponding sibling stage
implements its walker. The Go-parser stage (this workstream) does
not own that implementation — the file-level comments in both
parser files explicitly assign ownership elsewhere.

