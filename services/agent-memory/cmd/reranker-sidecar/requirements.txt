# reranker-sidecar runtime dependencies
# Pinned to mid-2024 versions known to work with
# cross-encoder/ms-marco-MiniLM-L-12-v2 (~33M params).

# HTTP layer
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1

# ML stack -- torch + transformers + sentence-transformers
# sentence-transformers' CrossEncoder wraps a torch
# transformer; the pinned version matrix below is the
# combination tested on python:3.11-slim.
torch==2.3.0
transformers==4.41.2
sentence-transformers==3.0.1

# Stage 8.3 observability surface (services/agent-memory/
# cmd/reranker-sidecar/observability.py). prometheus-client
# emits the Prometheus /metrics body; the opentelemetry
# trio exports spans over OTLP/HTTP when the env vars are
# set, otherwise the observability.py fallback installs a
# noop tracer so the same code path runs in dev.
prometheus-client==0.20.0
opentelemetry-sdk==1.24.0
opentelemetry-exporter-otlp-proto-http==1.24.0
opentelemetry-api==1.24.0
